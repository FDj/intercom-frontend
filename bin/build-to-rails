#!/bin/bash

OLD_ASSETS="../tapgig-mono/apps/rails/public/intercom_assets/assets"

if [ -d $OLD_ASSETS ] ; then
  vite build
  rsync -avz --delete dist/ ../tapgig-mono/apps/rails/public/intercom_assets/
else
  echo "$OLD_ASSETS not found, not syncing -- check your paths!"
fi

