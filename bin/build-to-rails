#!/bin/bash

OLD_EMBED_JS="../tapgig-mono/apps/rails/public/intercom_assets/embed.js"

if [ -f $OLD_EMBED_JS ] ; then
  vite build
  rsync -avz --delete dist/ ../tapgig-mono/apps/rails/public/intercom_assets/
else
  echo "$OLD_EMBED_JS not found, not syncing -- check your paths!"
fi

